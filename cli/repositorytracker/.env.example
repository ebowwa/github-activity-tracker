# GitHub Personal Access Token (Store in Doppler for security!)
# Get from: https://github.com/settings/tokens
# Required scopes: repo, user, read:org
# GITHUB_TOKEN=your-token-here  # Only use this for local dev, prefer Doppler

# Configuration (not secrets - safe to commit)
# Your GitHub username (if not provided, will fetch from API)
GITHUB_USERNAME=

# Optional: Organization names to track (comma-separated)
GITHUB_ORGS=

# Optional: Specific repos to track (comma-separated, format: owner/repo)
TRACKED_REPOS=

# Optional: Days to look back for activity (default: 7)
DAYS_BACK=7

# Optional: Cache directory
CACHE_DIR=.cache

# Optional: Output format (json, table, markdown)
OUTPUT_FORMAT=table